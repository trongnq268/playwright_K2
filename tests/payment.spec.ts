// tests/payment.spec.ts
import { test, expect } from '@playwright/test';
// Import helper vừa tách

import {PaymentData, calculateTotalAmount} from '../helpers/paymentHelper';

test('has title', async () => {
 
  
});